# Integration queue — PR #5 (Combat HUD + class-select)

**Status:** Ivo **PASS** @ `879a85c` — merge to `master`.
**Lane:** Claude Code / in-editor for UMG only. C++ bases are done.
**Asset go-ahead:** Do **not** create `.uasset` / `.umap` until Jacob records go-ahead on **this** ticket (ticket-2 placeholder approval does not cover HUD). MCP must answer on `:8000` before any Claude Code handoff.
**Detailed handoff (ships with PR):** `Docs/integration-queue/hud-extend.md`

## What landed in C++

| Class | Parent | Role |
| --- | --- | --- |
| `UCombatHUD` | `UCombatLifeBar` | Screen HUD: life + corruption stub + 4 cooldown wells |
| `UCombatAbilityCooldownSlot` | `UUserWidget` | One cooldown well (icon / fill / text) |
| `UCombatClassSelectScreen` | `UUserWidget` | Soft-refs Cole’s three `UPlayerClassData` paths |
| `UCombatLifeBar` | (existing) | `SetLifePercentage` / `SetBarColor` are `BlueprintNativeEvent`; `SetLifeFromAttributeSet` reads Health / MaxHealth |

World-space life bars on character/enemy WidgetComponents stay as-is — do not replace them.

## Bindings (Priya / Ivo verified)

| Surface | Bind |
| --- | --- |
| Life / health | `UCombatAttributeSet` **Health / MaxHealth** via `SetLifeFromAttributeSet` or `SetLifePercentage` |
| Corruption | **Unbound stub** until Gary’s attribute set lands (`Corruption` / `MaxCorruption` on draft #6). Do not invent a name in UMG |
| Cooldown slots 0–3 | Placeholder tags `Ability.HUD.Cooldown.Slot0`–`Slot3` until Gary’s class ability tags exist |

## In-editor wiring (Claude Code) — after Jacob go-ahead

1. `WBP_CombatAbilityCooldownSlot` — parent `CombatAbilityCooldownSlot`; optional `AbilityIcon` / `CooldownFill` / `CooldownText`. Leave `IconTexture` soft-ref for Beck.
2. `WBP_CombatHUD` — parent `CombatHUD`; bind `LifeMeter`, `CorruptionMeter` (layout only), `AbilitySlot_0`…`AbilitySlot_3`. Suggested path `/Game/Variant_Combat/UI/WBP_CombatHUD`. Spawn from `ACombatPlayerController` (`CreateWidget` + `AddToPlayerScreen`) — not wired in this C++ PR.
3. `WBP_CombatClassSelect` — parent `CombatClassSelectScreen`; `Button_Ordo` / `Button_Ignivarum` / `Button_Luminarch`, optional `Button_Confirm`, `Text_SelectedClassName` (**DisplayName only**). Suggested path `/Game/Variant_Combat/UI/WBP_CombatClassSelect`.
4. Soft-ref DA paths (assets from ticket **#4**, not this PR):
   - `/Game/Variant_Combat/Player/DA_PlayerClass_Ordo` → Igni Orthodoxia
   - `/Game/Variant_Combat/Player/DA_PlayerClass_Ignivarum` → Genitorii Gothica
   - `/Game/Variant_Combat/Player/DA_PlayerClass_Luminarch` → Luminarch Collegium

## Follow-ups (not blocking merge)

- After Gary **#6** lands: retarget corruption to `UAeterniiAttributeSet` Corruption/MaxCorruption (+ Depth 0–3); swap placeholder cooldown tags for real class ability tags.
- Cole’s three DA placeholders remain on ticket **#4** / MCP.
- No `.uasset` / `.umap` / `.umeta` in this PR (Lane A clean).
