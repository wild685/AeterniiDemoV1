# Integration queue — PR #2 (Ghurazi Horde AI)

**Status:** Ivo **PASS** @ `4f556ea` — merged to `master` @ `a6c44d5`.
**Asset go-ahead (2026-09-21, Jacob):** Placeholder BPs / StateTree / spawners / tell VFX are approved for a playable demo. Lore flavor and class copy do not need to be final yet.
**Lane:** Claude Code / in-editor. No further C++ required for this ticket.

## TODO (later — not blocking playable demo)

- [ ] **Lore / flavor pass:** Update Ghurazi / class display copy, Handoff Brief alignment, and any placeholder naming once 3D assets and lore embedding start. Jacob: current class info is close but not locked; defer until the art/lore design pass.

## What landed in C++ (already on master)

| Area | Types |
| --- | --- |
| Enemy | `AGhuraziHordeEnemy` (subclass of `ACombatEnemy`) — archetypes Raider / Chanter / Mauler |
| Spawner | `AGhuraziHordeSpawner` (subclass of `ACombatEnemySpawner`) — pack spawn + weighted classes |
| Hooks | Virtuals on `ACombatEnemy` / `ACombatEnemySpawner` for combo/charged/spawn reuse |
| Damage | Pack/horde hits stay on `ICombatDamageable::ApplyDamage` — **not** `UCombatAttributeSet` / GAS |

No new StateTree task/condition types. Existing combo / charged / IsInDanger / EQS Danger stay the contract.

## In-editor wiring (Claude Code) — placeholders OK

### Blueprints (parent `GhuraziHordeEnemy`, abstract C++)

1. Create `BP_GhuraziRaider` / `BP_GhuraziChanter` / `BP_GhuraziMauler`.
2. Set `Archetype`, click **Apply Archetype Defaults**, then mesh / anim BP / life bar widget (same as existing combat enemies — `BeginPlay` checks the life bar). Placeholder meshes are fine.

### Montages (reuse combat enemy layout)

- Combo: sections + `AnimNotify_CheckCombo` + `AnimNotify_DoAttackTrace`
- Charged: `ChargeLoopSection` / `ChargeAttackSection` + `AnimNotify_CheckChargedAttack` + `AnimNotify_DoAttackTrace`
- Until notifies exist, C++ fallback-strikes once after telegraph commit so PIE still deals damage.

### Telegraph VFX

Implement in `BP_OnTelegraphStarted` / `BP_OnTelegraphCommitted`:

- Thin contracting violet bracket (dodgeable)
- Chanter: warn ring at cached cast location (`GetCachedCastLocation()`, `GetTelegraphAlpha()`)

### StateTree `ST_GhuraziHorde`

AI Component schema — assign on existing `CombatAIController` BP or a thin child (**do not** fork a new AI component).

| Archetype | Flow | Post-attack Delay |
| --- | --- | --- |
| Raider | Combo *or* Charged → loop; optional danger evade | 1.35s |
| Mauler | Charged only; skip danger evade | 2.3s |
| Chanter | Charged only; kite if closer than ~400cm | 3.1s |

Pack desync: random Delay 0.3–1.1s on first entry. Root: `GetPlayerInfo` (Raider/Mauler MaxRange ~2500; Chanter engage ~640cm) → Face Towards Actor.

### Spawner

`BP_GhuraziHordeSpawner` parent `GhuraziHordeSpawner`: `PackSize=3`, weights e.g. Raider 0.70 / Chanter 0.15 / Mauler 0.15, `SpawnCount` = total individuals.

Place in a Combat / demo map so the player can fight a pack in PIE.

## PIE smoke

1. Place spawner in a Combat map, PIE — pack spreads, no stack.
2. Readable telegraph pause before damage (`LogGhuraziHorde`).
3. Chanter: stand during 1.35s tell, dodge the delayed strike at where you were.
4. Interrupt Raider mid-windup — tree must not hang.
5. Player melee still trips `NotifyDanger` on Ghurazi.

## Notes

- Damage routing settled with Gary: do **not** migrate horde melee/chant to GAS in a later pass.
- Jacob go-ahead recorded: placeholder assets for playable demo are authorized on this ticket.
