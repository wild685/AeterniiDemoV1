# Integration queue — PR #1 (GAS-lite boss combat scaffold)

**Status:** Ivo **PASS** (Build.bat EXIT=0 on `cursor/boss-combat-p1-df1b` @ `a9cbe17`). Ready to merge to `master`.
**Lane:** Claude Code / in-editor only. No further C++ required for this ticket.
**Do not** edit `.uasset` / `.umap` from Lane A bots — this ticket is the in-editor wiring pass.

## What landed in C++ (already in the PR)

| Area | Types |
| --- | --- |
| GAS | `UCombatAbilitySystemComponent`, `UCombatAttributeSet` (Health, MaxHealth, Stamina stub), `UGE_Damage_Melee` |
| Tags | `Ability.Attack`, `Ability.Dodge`, `Status.Dodge.IFrames`, `Data.Damage` |
| Boss GAs | `UGA_Boss_Sweep`, `UGA_Boss_Slam`, `UGA_Boss_Closer` |
| Player GA | `UGA_Dodge` |
| Pawns / AI | `ACombatBoss`, `ACombatBossAIController`, State Tree tasks (Acquire / Choose / Try Activate / Wait / Move Closer) |
| Notifies | `AN_TellStart`, `ANS_DamageWindow`, `AN_RecoverStart`, `AN_AttackEnd`, `ANS_DodgeIFrames` |
| Config | GameplayAbilities plugin on in `.uproject` + `Build.cs`; tags in `DefaultGameplayTags.ini` |

Full timings and chooser rules: `Docs/CombatP1.md`.

## In-editor wiring (Claude Code)

Suggested Content paths (create if missing):

- `/Game/Variant_Combat/Blueprints/BP_CombatBoss` — parent `CombatBoss`
- `/Game/Variant_Combat/Blueprints/AIC_Boss` — parent `CombatBossAIController`
- `/Game/Variant_Combat/AI/ST_BossCombat` — StateTree AI Component schema
- Montages: `AM_Boss_Sweep`, `AM_Boss_Slam`, `AM_Boss_Closer`, `AM_Dodge` under `/Game/Variant_Combat/GAS/Montages/`
- Optional BP children of the three boss GAs + `GA_Dodge` for montage refs

### Checklist

1. **BP_CombatBoss** — same Life Bar widget setup as other combat enemies; place `Hitbox_Melee` / `Hitbox_AOE`; leave collision disabled.
2. **Montages (Persona)** — sections exactly `Tell` → `Commit` → `Active` → `Recover` → `End` with timings in `Docs/CombatP1.md`; place tells/damage/recover/end notifies; dodge montage ~0.55s with `ANS_DodgeIFrames` 0.05–0.35s.
3. **Grant / assign** — assign montages on GA CDOs or BP children; keep `Default Abilities` complete if overriding the empty-array C++ fallback.
4. **ST_BossCombat** — wire Idle_AcquireTarget → ChooseAttack → ActivateAbility → WaitAttackEnd → Delay 1.0s → ChooseAttack; ChooseAttack fail → MoveCloser 0.75s → ChooseAttack. Assign on `AIC_Boss` → `StateTreeAI`.
5. **Player input** — create `IA_Dodge`, add to combat IMC, assign to `Dodge Action` on `BP_CombatCharacter`.
6. **PIE** — place boss in a Combat map; confirm `LogCombatGAS` ChooseAttack / TryActivateAbility; verify hit during Active and no-hit during dodge i-frames.

## Blockers / notes

- Attribute set here is `UCombatAttributeSet` (Health / MaxHealth / Stamina stub), **not** yet a separate `UAeterniiAttributeSet`. @Priya / @Gary should confirm whether HUD corruption binds wait on a later GAS PR or map to this set for now.
- Non-blocking: AbilityTags deprecation warning from Build.bat.
- Asset dispatch requires Jacob's recorded go-ahead on this ticket before Claude Code auto-runs `.uasset` / `.umap` work.
