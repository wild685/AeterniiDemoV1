# Integration queue — PR #4 (UPlayerClassData)

**Status:** Ivo **PASS** @ `c4a63e4` (Build.bat EXIT=0). Merge to `master`.
**Lane:** Claude Code creates the three DA `.uasset` instances; Priya soft-refs them; Gary fills GAS tags later.
**Detailed handoff:** also see `Docs/integration-queue/player-classes.md` (ships with this PR).

## What landed in C++

| Area | Detail |
| --- | --- |
| Type | `UPlayerClassData` (`UPrimaryDataAsset`, PrimaryAssetType `PlayerClass`) |
| Canon ids | `ordo` / `ignivarum` / `luminarch` |
| DisplayName only | Igni Orthodoxia / Genitorii Gothica / Luminarch Collegium |
| Archetype (code only) | Bulwark / Aggressor / NoeticRanged |
| Config | Asset Manager scan for `/Game/Variant_Combat/Player` + keep #1 GAS globals |

## In-editor wiring (placeholders OK — playable first)

Create Data Assets (not in this PR):

- `/Game/Variant_Combat/Player/DA_PlayerClass_Ordo`
- `/Game/Variant_Combat/Player/DA_PlayerClass_Ignivarum`
- `/Game/Variant_Combat/Player/DA_PlayerClass_Luminarch`

For each: set ClassId → **Apply Canon Defaults From Editor**. Leave CharacterClass / PreviewMesh / AbilityTag empty for now. Lore flavor polish rides `Docs/TODO-lore.md`.

## Notes

- Iso combat numbers are not Unreal cm / current MaxHP scale — convert when wiring movement/damage.
- Ability tags stay empty until Gary; HUD cooldown binds wait on those tags.
